# 贡献指南

Octogent 是一个实验性的个人项目，目前不会活跃地评审 Pull Request。如果你仍要提交，请保持改动小、有测试支撑、易于评审。

## 动手之前

- 阅读 `docs/` 里的相关文档
- 在增加新的表面积之前，先确认 API 或 UI 里是否已有该行为
- 在文档与产品叙事中保持 Claude Code 优先
- 不要把设想中的功能写成已经可用的样子

## 前置条件

- Node.js `22+`
- pnpm
- `claude`，用于受支持的代理工作流
- `git`，用于工作树功能

## 安装

```bash
pnpm install
```

## 开发

```bash
pnpm dev
```

开发启动器会同时启动本地 API 和 Web 应用。

## 必跑检查

提交 Pull Request 之前运行：

```bash
pnpm test
pnpm lint
pnpm build
```

需要重写格式时使用 `pnpm format`。

## 好的贡献长什么样

- 范围清晰的增量改动
- 行为变化配有测试
- 工作流或概念变化时，同一次改动里同步更新文档
- 代码与文档反映当前实现，而不是路线图

## 文档政策

- `docs/` 面向贡献者与未来的编码代理
- 如果你改动了触手、待办、终端、编排或消息传递，请更新对应的文档页

## Pull Request 期望

- 理解目前 Pull Request 不会被活跃评审
- 用一小段话说明问题
- 用具体的措辞说明行为变化
- 指出对持久化、API 或工作流的影响
- 可见的 UI 改动附上截图
- 如有代码由 AI 编写，披露所用的编码代理与模型
- 明确列出尚未完成的后续工作，而不是隐藏它

## 当前最重要的方向

- 触手模型与面向代理的上下文文件
- 待办解析与委派流程
- Claude Code 终端生命周期
- 子代理编排
- 代理间消息传递
- 修复既有问题并优化可靠性

> 本文件是 [CONTRIBUTING.md](CONTRIBUTING.md) 的中文翻译版本。如有歧义，以英文原文为准。
